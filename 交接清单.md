# 数迹（Digitrace）项目交接清单

> 交接时间：2026-08-15 · 由前一个对话整理，供新对话续接。
> 主仓库：`C:\Users\chen\Documents\Codex\2026-08-07\github-com-wellorbetter-timetrace\work\timetrace`
> 当前版本 **v2.27.0**（游戏统计 + 单应用日/周/月使用时长 + 加密版），已提交并推送；工作树当前仅保留本清单的后续待办更新。

---

## 一、项目目标（整体方向）

**数迹 Digitrace**：Windows 本地优先的使用统计与系统监控工具（Tauri 2 + Next.js + Rust）。
fork 自 wellorbetter/timetrace（MIT），现为公开仓库 `github.com/LINGYIIIIIIII/digitrace`。

核心价值主张：**数据全部本地存储不上传**（%APPDATA%\TimeTrace）+ **三件套架构**：

| 组件 | exe | 说明 |
|---|---|---|
| 完整版 | `digitrace.exe` | 全部功能：使用统计、网络/硬件监控、健康提醒、托盘、自动更新 |
| 独立监控 | `digitrace-monitor.exe` | 无界面常驻采集（CPU/内存/温度/网络），写共享内存，完整版不开也能跑 |
| Lite 查看器 | `digitrace-lite-viewer.exe` | 零依赖 Win32 小窗，读共享内存显示实时指标 |

近期主线：**仪表盘九宫格布局重构**（已基本完成，见下）。

---

## 二、已完成（当前程度）

### 0. 全量加密（本次会话，未提交）
- **用户决定范围**：只加密"身份字段"（app_name / exe_path / window_title / diary）+ config.json + 日志；
  时长/时间戳/分钟级指标保持明文（数字不具标识性，逐行 AES 成本高）。SQLCipher 方案已放弃
  （vendored OpenSSL 在 Windows MSVC 下编译失败，已回退 rusqlite bundled）。
- **密钥**：DPAPI（CryptProtectData，用户作用域）包裹的 32 字节随机主密钥，落盘
  `%APPDATA%\TimeTrace\key.bin`（OnceLock 缓存；换用户/换机器拷目录会不可读——特性而非缺陷）。
- **加密格式**：AES-256-GCM（aes-gcm 0.10 + rand 0.8）。字段 = `aes:` + Base64(nonce12+ct+tag)；
  整文件 = 魔数 `dgc1` + 同一 blob；op.log/timetrace.log = `dgc1` + u16 版本 + 逐条 [u16 长][blob]。
- **兼容**：旧 `dpapi:` 字段（field_decrypt 回退）、旧明文（原样透传）、旧明文 op.log
  （首次加密写入时轮转清空）、旧明文 config.json（无魔数视为明文）。
- **op.log Windows 坑（本次踩到并修复）**：`OpenOptions.append(true)` 会**覆盖** `write(true)`，
  句柄只剩 FILE_APPEND_DATA → `set_len` 报 ACCESS_DENIED。改为 read+write+`truncate(false)`，
  写入前手动 `seek(End(0))`；`clear()` 改为无条件 truncate+重建头部。
- **timetrace.log 加密**：`setup_logging` 改用 `EncryptedLogWriter`（tracing_subscriber 的
  MakeWriter），旧明文日志首次写入自动轮转。
- **新增「导出使用数据 CSV」**（设置 → 数据）：`export_usage_csv` 命令 → `%APPDATA%\TimeTrace\export\数迹使用数据-*.csv`
  （app/date/active_secs/idle_secs 全量，不含日记与窗口标题）；前端按钮 + 三语文案。
- **测试**：40 核心单测全过（字段往返/文件往返/篡改检测/旧格式兼容/oplog 加密 tail+clear）。

### 1. 仪表盘九宫格单元网格 v4（`db461b9` + `e6a4aa7` + 本次会话收尾）
- 布局模型从「3 列流水 + sm/md/lg 三档」改为**九宫格单元网格**：
  - 3 列 × 统一行高（行高 = 列宽 × 0.92，近 1:1 单元），`grid-auto-flow: row dense` 自动回填空洞
  - 尺寸 7 档：`1x1 / 1x2 / 2x1 / 2x2 / 3x1(整行窄) / 3x2(整行高) / 3x3(整屏)`
  - 4 套模板（均衡/紧凑/展示/经典），每套都是"完整矩形"无空洞；编辑弹窗可一键切换
  - 编辑缩略图 = 同款网格（1:1），拖拽换位 + 手柄调尺寸 + 隐藏/恢复
- **修复关键 bug**：Tailwind v4 purge 掉动态拼接的 `col-span-${n}` 类名 → 卡片全坍缩 1×1。
  改为字面量映射表 `SPAN_CLASSES`（dashboard-layout.ts 内），已修复。
- 交互修复（`e6a4aa7`）：
  - 手柄尺寸循环 `SIZE_CYCLE` 加入 `3x1`（横长整行档）——之前缺它导致拖不到横长格式
  - 表格类内容（应用列表/按应用流量/实时网络应用/磁盘温度）从"固定行距+滚动"改为
    **flex 行距自适应**：内容少拉宽填满、内容多压缩、极端才滚动
- **手柄交互升级（本次会话）**：SIZE_CYCLE 固定循环 → **2D 方向吸附拖拽**：
  - 左右拖改变宽度（列数）、上下拖改变高度（行数），可对角同时变两维
  - 死区 0.25 格防误触，之后每拖满 1 格换一档、可连续跨档（1x1 右拖 2 格直达 3x1）；
    越界自动吸附到最近合法档位（窄列最高 2 行，整行宽才允许 3 行；1x3/2x3 仍不存在）
  - 拖拽时悬浮徽标实时显示目标尺寸；手柄改为可聚焦按钮，支持键盘方向键调尺寸
  - 指针捕获（setPointerCapture）：拖出窗口/弹窗外松手不卡死
  - 纯函数集中在 `dashboard-layout.ts`：`snapDelta` / `sizeFor` / `resizeByDelta`，
    已加单测（`dashboard-layout.test.ts`，5 用例）
- 已记录待办（用户确认以后再处理）：
  - 问题 2「使用时长用柱状图」：用户已撤销，不需要改
  - 竖长 1×3：用户明确**不要**，只要横长 3x1

### 3. 双击"没反应"修复（本次会话，`0dda4d3`，已推送）
- **根因**：运行中的实例以管理员权限驻留托盘（管理员自启/提权重启）时，双击同路径 exe
  走单实例回调唤出窗口，被 Windows 完整性级别隔离（UIPI）拦截 → 静默无反应
- **修复 A（同路径唤出）**：双击时检测到同路径实例在跑 → 写 `%APPDATA%\TimeTrace\show_request.json`
  标记 → 运行中实例轮询（spawn_takeover_poller）到后**自己弹出主窗口**（走文件系统，跨权限可用；
  `--tray` 自启不写、15s 新鲜度防陈旧误触发）
- **修复 B（切换继承管理员权限）**：`switch_to_pending` 旧版是管理员时新版也以 RunAs 拉起
  （UAC 一次），避免"切换后变普通权限、与管理员自启割裂"（本次事故根源）
- 新 exe：`outputs\digitrace-v2.26.1-双击修复.exe`（含九宫格 + 手柄 2D + 双击修复）
- **遗留风险**：切换弹窗（TakeoverDialog）由旧版**前端**渲染——若旧版前端未加载则弹窗不出现，
  新版等 3s 后弹"检测到旧版…未能自动切换"提示（有兜底，但体验一般，可后续改原生弹窗）

### 4. 实时速率曲线裁切 + 纵轴缩放（本次会话，`8fcc1dc`，已推送）
- **根因**：九宫格 v4 用 `CHART_ASPECT`（16:9/21:9）按**卡片宽度**算图表高度，但九宫格行高
  有限（≈120px/行）→ 图表超高 → CardShell `overflow-hidden` 裁掉下半截，横轴消失
  （2x1/3x1/2x2 全部中招，用户实测"只显示上半部分"）
- **修复**：实时曲线 + 小时曲线改为 `min-h-0 flex-1` 填满卡片剩余高度（横纵轴同时可见）；
  纵轴显式 domain 按数据峰值自动缩放（`niceCeil` 1/2/5×10ⁿ 档 + 10% 余量 + 512KB/s 兜底），
  窄卡（1x1/1x2）轴宽自适应 46px；
  **恢复滚轮纵轴缩放**（上滚放大/下滚缩小，`passive:false` 阻止页面滚动，双击复位）
  + 图表右上角**缩放选择栏**（＋/−/复位/自动标识），三语文案已加
- 新 exe：`outputs\digitrace-v2.26.1-图表修复.exe`（含此前全部修复）
- 说明：git 全历史检索确认仓库里从未有过滚轮缩放代码（v2.19.1 折叠前上游不可考）；
  用户认为"滚轮被删"，实际是九宫格重构把图表改坏 + 无缩放入口——本次一并补齐

### 2. 三件套架构（v2.26.0，已发布）
- `crates/metrics`：共享内存实时指标（`%APPDATA%\TimeTrace\metrics.map`，4096B，
  语言无关，C 头 `metrics.h`）；**多写者安全**：命名互斥串行化 + seq 全局单调
  （完整版与独立监控并发写不冲突）
- `crates/monitor` → `digitrace-monitor.exe`：无 UI 常驻采集，单实例，`--stop` 退出
- `crates/metrics-viewer` → `digitrace-lite-viewer.exe`：零依赖 Win32/GDI，~240KB；
  启动时数据陈旧会自动拉起同目录 monitor
- Release 自动发布 6 资产（完整版 + Lite + monitor，各带 sha256）

### 3. 自动更新（GitHub Releases 直连）
- 读 `api.github.com/repos/{owner}/{repo}/releases/latest`，tag=版本，`.exe`+`.sha256` 强校验
- 静默更新（`silent_pending.txt` + 退出时 PowerShell 换 exe 重启 --tray）
- 固定检查时刻（`update_check_hour`），每天最多查一次
- **Release notes 已改为从 CHANGELOG 自动提取**（`cfea0ab`，v2.26.0+ 附组件清单）

### 4. 文案审计（`68167b9`）
- i18n 三语（zh-CN/en-US/ja-JP）364 key 完全对齐；ja 补 13 缺失 key；品牌统一
- Lite/monitor README、根 README、metrics README 与新三件套架构同步
- GitHub 仓库 description + topics 已更新；历史 Release notes 已回填

### 5. 性能与底层（v2.24.x ~ v2.25.x 时期）
- 仪表盘秒级实时跳动、全年热力图按年加载、10s 轮询只刷仪表盘
- 网络监控复用 sysinfo 进程表（内存优化）
- 硬件/温度分钟级历史（monitor.db `metric_samples`）

---

## 三、下一步（待办与方向）

### 短期（仪表盘收尾）
1. ~~**真机验收 + 发版**~~：v2.27.0 已完成构建、测试、打 tag 并推送；完整 exe 已按交付习惯自动拉起。
   后续版本继续遵守 **bump 版本 → 打 tag → 触发自动 Release**。
2. ~~手柄拖动体验优化~~（已完成，见上「手柄交互升级」）
3. **切换弹窗改原生**（可选）：TakeoverDialog 依赖前端渲染，旧版前端未加载时弹窗不出现；
   可改为旧版轮询到标记后用原生 MessageBox（win32）询问，更稳
4. ~~**NVML 崩溃保护**~~：Windows 事件日志确认 `2026-08-18 17:10:24` 旧版
   `digitrace-v2.26.1-加密版.exe` 在 `nvml.dll` 中发生 `0xc0000005` 并退出。现已移除
   主进程内 NVML 调用，改为隔离的 `nvidia-smi.exe` 子进程，2 秒超时、失败后 60 秒熔断；
   GPU 快照 1 秒缓存，驱动异常仅导致 GPU 指标缺失，不会终止数迹进程。

### 中期（潜在方向，需用户确认优先级）
5. ~~**功耗采集并入硬件曲线**~~：已完成 GPU 板卡功耗采集（瓦特），通过隔离的
   `nvidia-smi.exe` 读取，写入硬件分钟级历史，并合并到实时硬件曲线和日面板硬件曲线；
   驱动不可用或异常时记录缺失值，不影响主进程。当前不伪造 CPU/整机功耗。
6. **完整版与独立监控的协同策略（P1）**：当前两者会同时采集、写共享内存和写 `monitor.db`，
   造成 last-writer-wins、分钟桶重复加权和来源抖动。下一步做命名互斥租约的单采集者，另一方只读，
   并覆盖双进程接管、持有者崩溃恢复和管理员/普通权限组合测试。
7. **历史 Release 说明回补**：实际缺正文的只有 `v2.23.0`、`v2.23.1`；只用 `gh release edit`
   定向更新 notes，禁止重推旧 tag 或覆盖旧资产。

### 已上线（v2.27.0）
- **游戏统计/连续游戏提醒**：平台识别、游戏会话统计、连续游戏提醒与仪表盘卡片。
- **单应用周期使用时长**：应用统计页和仪表盘详情显示今日/本周/本月活跃时长。
- **加密与 CSV 导出**：身份字段、配置、日志加密；设置页支持导出使用数据 CSV。
- **网络实时曲线保留窗口**：后端秒级环形缓冲（默认 5 分钟，设置页可配 60~600s，
  重启清零）；`get_network_live_window` 命令，仪表盘卡/网络页共用，切页历史不丢
- **日历重构**：月/年视图 contain-fit 自适应窗口（1:1 日期格，居中无滚动，UI 缩放兼容）；
  **日面板仪表盘**（点日期进入）：24h 活跃柱状 + 应用 Top6 堆叠 + 硬件曲线（CPU/内存/温度/GPU 功耗）
  + 网络曲线 + 会话列表；新增 `get_day_metrics`（monitor.db 分钟级历史按日查询）
- 新 exe：`outputs\digitrace-v2.26.1-实时与日历.exe`（含此前全部修复）

### 本轮已完成（待下个版本发布）
- **GPU 功耗记录**：实时快照、独立监控与完整版均采集 GPU 板卡功耗，并写入
  `monitor.db` 分钟级 `gpu_power_watts`；硬件页和日面板统一展示功耗曲线。
- **FPS 规划移除**：不采集、不在 Lite 查看器展示；共享内存中的旧预留字段仅为 ABI 兼容保留。

### 遗留
- ~~日面板每次进入拉 24 次 `get_hour_apps`~~：已改为单次 `get_day_hour_apps` 批量查询并保留跨小时语义
- 日历"返回日历"回月视图；年视图点月份回月视图

### 长期
6. 数据迁移安全：`%APPDATA%\TimeTrace` 历史目录名（TimeTrace 字样）保留是刻意的（兼容+致谢 MIT），勿改
7. 用户提问过"底层监控是否拆独立进程"→ 已按用户决定：完整版不动 + 新增独立监控（当前形态）

---

## 四、环境与操作备忘（重要）

- **推送必须用 SSH deploy key**（沙箱 schannel TLS 被禁）：
  ```powershell
  $env:GIT_SSH = 'C:\Users\chen\Documents\Codex\2026-08-07\github-com-wellorbetter-timetrace\work\git-ssh.cmd'
  git push git@ssh.github.com:LINGYIIIIIIII/digitrace.git main
  ```
- **gh CLI 全路径**：`C:\Users\chen\AppData\Local\Microsoft\WinGet\Links\gh.exe`
- **版本纪律**：bump 用 `scripts\bump.ps1 新版本号`（改 src-tauri/Cargo.toml + tauri.conf.json）；
  CHANGELOG 加顶部条目；`cargo fmt --all` 必须在 commit 前（CI 强制）
- **CI 要求**：前端 `next build` 先于 cargo build（generate_context! 需要 frontend/dist）
- **本地验证流程**（避开用户运行实例的单实例互斥）：
  临时改 tauri.conf.json identifier → 独立 `--target-dir` 构建 → 启动 `--show-window` 验证 →
  验证后**必须恢复 identifier 为 `com.digitrace.app`** + 清理 target 目录
- **沙箱**：对 %APPDATA% 写入会被拦 → 端到端验证共享内存时把 `APPDATA` 环境变量指到 workspace 内目录
- **用户环境**：用户在中国（npmmirror、360 可能误报）；当前用户机器上正式版需手动更新到新 exe
- **交付习惯（用户明确要求，务必遵守；2026-08-15 更新）**：版本更新**确认做完后直接启动 exe**
  交付（`Start-Process` 拉起，用户无需手动找文件/双击），**不再粘贴文件路径**；
  若旧实例在跑，拉起新版会走版本切换接管流程（旧版弹"是否切换"对话框）。
  产物目录在**仓库外**：`C:\Users\chen\Documents\Codex\2026-08-07\github-com-wellorbetter-timetrace\outputs\`

## 五、当前文件状态

- v2.27.0 功能改动已收敛到发布提交 `28dadcd`；本次仅更新本交接清单中的后续待办。
- 发布包目录：`outputs\digitrace-v2.27.0`（完整版、Lite viewer、monitor，均带 SHA-256）。
- 前端验证已过：`npm run typecheck`、`npm run build`。
- 后端验证已过：`cargo check --workspace`、`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets -- -D warnings`。
- workspace 测试 53 通过、3 个 DPAPI 沙箱失败（`0x80070002`，与本次功能无关）。
- 远程已推送：`main` 与 `v2.27.0` tag；GitHub Actions Release 已触发。

---

## 六、涉及的核心文件

| 文件 | 说明 |
|---|---|
| `crates/core/src/security.rs` | 主密钥（DPAPI）+ AES-256-GCM 字段/文件加解密（aes:/dgc1 格式 + 旧格式兼容） |
| `crates/core/src/config.rs` | AppConfig 整文件加密读写（无魔数回退明文） |
| `crates/core/src/oplog.rs` | 加密操作日志（dgc1 记录格式、轮转、tail_lines 解密、EncryptedLogWriter） |
| `src-tauri/src/api/mod.rs` | setup_logging 用 EncryptedLogWriter 加密 timetrace.log |
| `src-tauri/src/api/impl_dashboard.rs` | export_usage_csv（全量使用数据 → export 目录 CSV） |
| `frontend/src/app/components/dashboard/dashboard-layout.test.ts` | 吸附纯函数单测（5 用例） |
| `frontend/src/app/components/dashboard/DashboardPage.tsx` | 网格渲染 + 编辑弹窗 + 模板切换 + 手柄 2D 拖拽/键盘调尺寸/尺寸徽标 |
| `frontend/src/app/components/dashboard/cards.tsx` | 15 张卡片组件（表格自适应改动在这） |
| `frontend/src/app/components/NetAppsCard.tsx` / `AttributedUsageCard.tsx` | 表格自适应改动 |
| `crates/metrics/src/lib.rs` | 共享内存发布/读取 + 多写者互斥 |
| `crates/monitor/src/main.rs` | 独立监控进程 |
| `crates/metrics-viewer/src/main.rs` | Lite 查看器 |
| `src-tauri/src/update.rs` | 自动更新（GitHub 直连/静默/固定时刻） |
| `.github/workflows/release.yml` | 自动发布（6 资产 + CHANGELOG 提取 notes） |
| `scripts/bump.ps1` | 版本升级脚本 |
