# 数迹（Digitrace）更新日志

按版本归档每次功能更新与修复（最新在前）。纯构建产物更新不单独记录。

## v2.24.0（2026-08-14）
- 性能优化：仪表盘不再每次刷新都导出「全年 CSV」——新增轻量按天聚合查询 `get_year_heatmap`，
  全年热力图仅在年份变化时加载；10 秒轮询只刷新仪表盘数据与今日小时分布，页面不再卡顿、数据实时更新
- 内存优化：按应用网络监控复用 sysinfo 进程表（原每 2 秒重建 System，现只刷新所需字段），
  减少内存抖动与 CPU 尖峰；移除无引用的 `SysinfoProcessQuery` 死代码
- 修复 `scripts/bump.ps1` 在 Windows PowerShell 5.1 下不写 Cargo.toml 的问题

## v2.23.1（2026-08-14）
- 修复「检查更新」失败：检查阶段不再下载 `.sha256` 校验文件（只确认其存在），
  实际下载更新时才拉取校验——检查不再被网络波动卡住
- 校验文件下载增加重试与超时；失败时错误信息附带 curl 详情，便于定位

## v2.23.0（2026-08-14）
- 新增「自动发布流水线」：推送 `v*` tag 即自动构建、生成 SHA-256、发布 GitHub Release（exe + 校验文件）
- 新增 `scripts/bump.ps1` 一键升版本号；CI 增加 Cargo/tauri.conf/CHANGELOG 三处版本一致性校验
- 新增前端单元测试（vitest：日期时区工具、启动流程 store），CI 接入 `npm run test`
- 新增「固定检查时刻」：可设置每天固定小时检查更新（默认不固定：启动后 6 秒 + 每 6 小时轮询）
- 安全收尾：生产构建关闭 DevTools（配置 + 命令 + 设置入口 + 调试面板全部移除）；
  `open_external_url` 由 `cmd start` 改为 `ShellExecuteW`，消除命令注入面
- 移除未使用依赖 `react-icons` / `radix-ui` / `shadcn`（package.json；锁文件由 `npm install` 同步）
- 磁盘清理：删除 debug 编译缓存与 `.next` 缓存（约 16.5GB，自动重建）

## v2.22.0（2026-08-14）
- 新增「静默更新」开关（设置 → 更新）：后台自动下载新版本，退出应用时静默替换并以托盘模式重启，
  全程不弹确认框、不弹通知、无控制台窗口；关闭时保持原有手动确认流程
- 更新器子进程（curl / PowerShell / schtasks）全部补 `CREATE_NO_WINDOW`，彻底消除更新时的黑窗闪现

## v2.21.1（2026-08-14）
- 修复检查更新/下载时闪现黑色控制台窗口：更新相关的全部 curl 子进程补 `CREATE_NO_WINDOW` 静默运行
- 「检查更新」按钮同时加入「设置 → 更新」页（原仅在「关于」页），抽成共用组件

## v2.21.0（2026-08-14）
- 新增「GitHub 公开仓库直读更新」（类似 THRM）：设置「GitHub 仓库」后，应用直接读取该仓库
  最新 Release 获取新版本——tag 作版本号、`.exe` 附件作安装包、`.sha256` 附件作强校验；
  优先于更新清单地址，仓库必须公开（私有无法匿名读取）
- 更新器重构：清单模式与 GitHub 模式共用同一套下载/校验/接管流程；校验文件缺失一律拒绝

## v2.20.0（2026-08-14）
- 工程化整理与安全加固：
  - 更新器：清单缺少 `sha256` 一律拒绝；清单与下载地址强制 HTTPS（防降级与篡改）
  - ETW 网络事件解析改用索引构造，畸形事件不再导致进程崩溃
  - 数据目录初始化失败时弹窗提示原因，不再静默退出
  - 配置写入增加输入钳制（轮询间隔/阈值范围校验）
  - 移除 22 个无界面调用的遗留命令（日记、启动项管理器、暂停追踪、日志读写）
  - 清理 Flutter/bridge/egui/tui 遗留代码与构建残留，仓库瘦身（250 → 137 文件）
  - `Cargo.lock` 纳入版本管理；CI 增加 clippy（0 警告）、rustfmt、依赖漏洞审计
  - Bundle ID 更新为 `com.digitrace.app`

## v2.19.1（2026-08-12）
- 修复硬件监控页卡顿/未响应：磁盘健康改为自动一天一次（后端 24 小时缓存）+ 卡片手动「刷新」按钮，不再跟随 2 秒实时刷新反复拉起 PowerShell；查询失败时静默降级，不再拖垮整个页面。

## v2.19.0（2026-08-12）
- 新增「磁盘健康」：硬件监控页展示每块物理磁盘的健康状态（正常/警告/异常）、温度、SSD 磨损、通电时长与累计读写错误；数据来自 Windows 存储可靠性计数器，只读、不修改任何文件。

## v2.18.4（2026-08-12）
- 修复开机自启失效：自愈逻辑不再剥掉 `--tray`（否则自启会弹主窗口而非静默进托盘）；管理员自启计划任务在启动时自动用当前 exe 刷新路径（升级/移动后不再指向旧文件）；托盘勾选状态与设置页保持一致（注册表或计划任务任一存在即显示开启）。

## v2.18.3（2026-08-12）
- 修复打开设置页时快速闪现多个控制台窗口：查询计划任务自启状态与打开外部链接的进程改为 `CREATE_NO_WINDOW` 静默运行。

## v2.18.2（2026-08-12）
- 关于页许可证徽章改为「GPL-3.0」并链接到数迹 GitHub 仓库（`github.com/LINGYIIIIIIII/digitrace`），点击用默认浏览器打开。

## v2.18.1（2026-08-12）
- 构建改进：静态链接 MSVC 运行库（`+crt-static`），单 exe 不再依赖 `vcruntime140.dll` 等系统库，拷到任意 Windows 10/11 可直接运行（WebView2 为系统组件，Win11 自带 / Win10 随 Edge 自动安装，缺失时应用会提示安装）。

## v2.18.0（2026-08-12）
- 新增「开机自启以管理员权限运行」：注册 Windows 计划任务（RunLevel 最高权限），登录后数迹静默以管理员身份启动，开机即可读取 CPU 温度等硬件数据，无需每次手动提权（首次开启需一次 UAC 授权）。
- 设置页「启动与托盘」新增对应开关；关闭时自动清理计划任务并回退普通注册表自启，避免双开。

## v2.17.6（2026-08-11）
- 修复顶栏及窗口控制按钮出现橙红焦点框的问题：焦点圈改为仅在真实键盘导航（Tab/方向键）时显示，鼠标点击与窗口重新聚焦（截图/切换窗口）不再触发。

## v2.17.5（2026-08-11）
- 修复仪表盘小卡片被同行高卡纵向拉伸、不再是 1:1 方形的问题（网格默认拉伸改为 `self-start` + `min-h-0`，小卡保持正方形，内容在卡内滚动）。

## v2.17.4（2026-08-11）
- 修复顶栏缩放面板的百分比下拉被面板盖住（下拉层级 z-50 → z-10001，浮在面板之上）。

## v2.17.3（2026-08-11）
- 顶栏缩放面板改为悬浮在窗口最顶层（portal 挂载 + 固定右上角 + 强化背景/阴影），不再被内容卡片盖住或显得"嵌在页面里"。

## v2.17.2（2026-08-11）
- 撤销缩放 <100% 时的纯黑背景（恢复玻璃模糊）；修复顶栏缩放面板——预设下拉不再被误判为外部点击而还原，面板层级提高不被卡片盖住。

## v2.17.1（2026-08-11）
- 统计类卡片字体统一放大：仪表盘统计卡、流量统计、按应用统计、日历、硬件/温度表盘、健康卡等紧凑卡数值与标签放大一档，健康提醒页统计卡数值放大到 24px、图标与内边距加大，消除"卡大字小留白多"。

## v2.17.0（2026-08-11）
- 健康提醒状态持久化：今日提醒次数、上次提醒时间、上次休息时间跨重启保留；连续时长短间隔（≤90 秒）接着计时，长时间关闭重新计时；跨天正常清零。

## v2.16.11（2026-08-11）
- 缩放逻辑重构：缩放只作用于内容区，顶栏不缩放常驻；顶栏新增缩放面板（预览 + 确定/取消，像 Windows 一样保存生效）；设置页缩放支持数字输入与 75/100/125/150 快捷预设；缩小时窗口底部铺暗色背景，不再露出透明方框。

## v2.16.10（2026-08-11）
- 界面整体无极缩放（75%–175%）：字体、卡片、图表等比缩放；拖动时顶部弹出浏览器式百分比浮层。

## v2.16.9（2026-08-11）
- 按应用统计卡（小卡）改为上下各占 50%：柱状图与列表均分填满 1:1 卡片，列表显示 6 个应用。

## v2.16.8（2026-08-11）
- 健康提醒、统计卡内容行自动拉伸填满 1:1 卡片，消除底部空白。

## v2.16.7（2026-08-11）
- 恢复小卡 1:1 方形排版（保留卡内内容随卡片填满），移除网格强制纵向拉伸。

## v2.16.5（2026-08-11）
- 窗口缩放布局改进：小卡不再强制 1:1 畸形拉伸，流量统计 2×2 与日历内容随卡片高度自动填满，曲线图保持内容 1:1。

## v2.16.4（2026-08-11）
- 修复仪表盘图表坐标轴被裁剪：负边距改回非负，轴标签不再跑出画布。

## v2.16.3（2026-08-11）
- 修复启动首帧纯黑闪烁：前端渲染完首帧后再显示窗口，窗口不再空壳闪现。

## v2.16.2（2026-08-11）
- 温度聚合卡改为硬件顶栏同款半圆表盘：CPU/GPU 双色弧（温度+占用）、磁盘温度单弧。

## v2.16.1（2026-08-11）
- 开发者工具（DevTools）：设置中一键打开，点选界面元素实时调整样式；附带 UI 调试面板滑块。

## v2.15.9（2026-08-11）
- 内存优化：进程表查询缓存复用，托盘退出前销毁窗口清理 WebView2 子进程残留。

## v2.15.8（2026-08-11）
- 修复托盘幽灵图标：通知图标改用自增唯一 ID、添加前先清理、应用退出时统一删除。

## v2.15.7（2026-08-11）
- 按应用流量接入 ETW 实时数据：管理员下显示每应用实时速率与累计（合计模式，ESTATS 不可用时兜底）。

## v2.15.6（2026-08-11）
- ETW 事件解析修正：PID 4 字节、size 位于 offset 4（实测 7×u32 布局）。

## v2.15.5（2026-08-11）
- ETW 验证版加调试输出：日志 dump 事件原始字节与总字节数，定位 size 字段解析。

## v2.15.4（2026-08-11）
- ETW 内核网络事件采集验证版：管理员下订阅 Kernel-Network 事件，日志每 30 秒输出每进程速率摘要。

## v2.15.3（2026-08-11）
- 按应用流量探测扩大到全部活跃连接，日志统计不支持（50）数量，判断是否为环境限制。

## v2.15.2（2026-08-11）
- 按应用流量字节模式：绕过库绑定按官方签名直调 iphlpapi（修复 Offset 参数错位与枚举值错误）。

## v2.15.1（2026-08-11）
- 按应用流量字节模式：连接结构先清零再填（消除 padding 未初始化）、ROD 缓冲增至 1024 字节、日志区分 Set/Get 失败环节。

## v2.15.0（2026-08-11）
- 时间范围与时区设置（全局统一）：网络历史改为 24h / 今日 / 本次启动 / 7 天 / 30 天，采样与查询按配置时区，设置新增日期时区选项。

## v2.14.11（2026-08-11）
- 修复按应用流量字节模式读取缓冲区：ROD 缓冲区改用 256 字节固定数组并按 C 布局解析。

## v2.14.10（2026-08-11）
- 修复按应用流量字节模式：Set/Get 的 RW 缓冲区改用正确的 2 字节 v0 结构。

## v2.14.9（2026-08-11）
- 按应用流量字节模式：探测改为单连接成功即启用，并记录权限/错误码日志便于定位。

## v2.14.8（2026-08-10）
- 小卡片整体 1:1 方形并适配内容布局：统计卡竖排紧凑时长、硬件表盘迷你横排、流量/聚合卡精简小档、日历大卡限宽居中、中卡表盘横排。

## v2.14.7（2026-08-10）
- 曲线图高度随卡片宽度自适应：小卡 1:1 方形、中卡 16:9、大卡 21:9，避免窄卡里曲线只占上半部分。

## v2.14.6（2026-08-10）
- 修复缩略图编辑器拖拽排序：主窗口开启 HTML5 拖放，拖拽改为悬停高亮 + 松手落位。

## v2.14.5（2026-08-10）
- 修复管理员权限在版本交接时丢失：交接流程携带 elevated 标志，旧版用 RunAs 拉起管理员新版。

## v2.14.4（2026-08-10）
- 主窗口记忆大小/位置/最大化状态，更新重启后按上次布局恢复。

## v2.14.3（2026-08-10）
- 托盘气泡通知改为同步调用并写入操作日志（定位通知未弹出的原因）。

## v2.14.2（2026-08-10）
- 健康提醒通知改回 Windows 原生托盘气泡样式（移除右上角自绘透明通知窗口）。

## v2.14.1（2026-08-10）
- 仪表盘编辑改为可视化缩略图：拖拽排序、右下角手柄调大小、眼睛隐藏、已隐藏卡片抽屉一键恢复、恢复默认布局。

## v2.14.0（2026-08-10）
- 仪表盘只保留紧凑型布局：移除模板切换，旧布局自动迁移紧凑型，小卡排版优化，layout.ts 改名避免 Next.js 特殊文件冲突。

## v2.13.1（2026-08-10）
- 修复磁盘温度 PowerShell 查询闪现蓝色终端窗口（漏加 CREATE_NO_WINDOW）。

## v2.13.0（2026-08-10）
- 仪表盘模板化：4 套模板、三档卡片尺寸、聚合卡（硬件/温度/网络/时长）与互斥逻辑、旧布局自动迁移。

## v2.12.0（2026-08-10）
- 通知改应用内自绘（右上角 + MiSans）、字体选项加 MiSans、磁盘温度增加存储可靠性计数器后备。

## v2.11.2（2026-08-10）
- 新旧版自动交接：普通版双击遇旧版时旧版自动弹窗确认并拉起新版（跨管理员权限兜底）。

## v2.11.1（2026-08-10）
- CPU 温度改为各核平均 + 表盘说明行、每核详情放回原位、NVMe 温度日志页直读（磁盘温度修复）。

## v2.11.0（2026-08-10）
- 半圆双弧表盘（橙=温度/蓝=占用，大值底层小值顶层）+ 内存半圆 + CPU 每核详情 + 磁盘温度管理员权限读取修复。

## v2.10.2（2026-08-10）
- 内存占用进度条改细（更低调）。

## v2.10.1（2026-08-10）
- 双击新版遇到管理员旧版在线时弹提示先退出旧版（不申请管理员权限）。

## v2.10.0（2026-08-10）
- 硬件监控半圆表盘（占用率弧 + 温度数字，含 GPU 占用率 NVML）、实时刷新间隔 1 秒选项、重启/更新后强制展开主窗口。

## v2.9.0（2026-08-10）
- 温度监控（GPU NVML / 磁盘存储温度 / CPU PawnIO 可选内核驱动）+ 托盘温度行。

## v2.8.5（2026-08-10）
- 「启动显示主界面」正面表述替代「启动隐藏」。

## v2.8.4（2026-08-10）
- 启动隐藏设置 + 交接框「切换后打开主界面」选项 + 延迟拉起消除竞态。

## v2.8.3（2026-08-10）
- 托盘悬停提示改为版本号 + 数据行分行，右键菜单只留操作项。

## v2.8.2（2026-08-10）
- 新版接管改为应用内风格确认框（单实例交接，替代系统弹窗）。

## v2.8.1（2026-08-10）
- 托盘开关空数组误回退默认值 + 接管确认/失败提示增强。

## v2.8.0（2026-08-10）
- 托盘菜单规整重排 + 设置可配置托盘显示内容 + 末尾显示版本号。

## v2.7.8（2026-08-10）
- 按应用流量空名条目显示「未归属流量」兜底。

## v2.7.7（2026-08-10）
- NT 路径转换去掉尾部空字符（图标可正常提取）+ 兜底覆盖 NT 路径。

## v2.7.5（2026-08-10）
- 按应用流量图标：NT 路径转盘符路径 + 运行进程兜底补图标。

## v2.7.4（2026-08-10）
- 设置页显示联网检查状态（未配置地址 / 自动开 / 自动关）。

## v2.7.3（2026-08-10）
- 新版启动自动接管旧版（确认后退出旧版再启动）+ 构建脚本自动归档旧版本。

## v2.7.2（2026-08-10）
- 按应用流量（官方数据）显示 exe 名与图标。

## v2.7.1（2026-08-10）
- 健康提醒控件改为绑定即时配置，消除按钮 2 秒轮询延迟。

## v2.7.0（2026-08-10）
- DPAPI 敏感数据加密 + 自动更新 + 明文导出。

## v2.6.0（2026-08-10）
- 新增健康提醒页（连续使用检测 + Windows 原生通知）。

## v2.5.3（2026-08-10）
- 接入 Windows 官方按应用流量接口（GetAttributedNetworkUsageAsync，免管理员）。

## v2.5.2（2026-08-10）
- 按应用流量改为免管理员连接统计（字节模式需管理员，自动降级）。

## v2.5.1（2026-08-10）
- 开机静默自启（start_minimized 生效 + 统一托盘/设置自启机制 + 清理旧版自启记录）。

## v2.5.0（2026-08-10）
- 按应用网络流量监控（仪表盘 + 网络页）。
