# 版本号规则（每次改动都必须更新）

每次提交代码改动时，必须同步更新版本号（当前技术栈下共两处）：

- `src-tauri/Cargo.toml` → `version = "x.y.z"`
- `src-tauri/tauri.conf.json` → `"version": "x.y.z"`

> 历史说明：早期 Flutter 时代的版本号位于 `app/pubspec.yaml` 等三处，Tauri 迁移后已统一为以上两处。

## 规则

1. **小改动**（bug 修复、文案/样式微调、小功能）：补丁号 +1，例如 `2.17.6 → 2.17.7`。
2. **中等改动**（新功能、界面模块级更新）：次版本 +1，例如 `2.17.0 → 2.18.0`。
3. **大型改动**（重构、架构级变更、不兼容）：主版本 +1，例如 `2.x.x → 3.0.0`。

## 配套要求

- 每次改版本号时同步更新 `CHANGELOG.md`（最新在前，记录功能/修复）。
- 编译终端最后一行输出本次构建的版本号，便于确认打包的是哪个版本。
- 发布产物为单文件 exe：`cargo build --release` 后取 `target/release/timetrace.exe`。
